Switch the factory-wide contested-output policy from shortage-first to fifo.

Expected effect: Test whether deterministic output arbitration reduces starvation or blockage in the measured topology; shortage-first uses destination batch coverage and objective critical depth.
