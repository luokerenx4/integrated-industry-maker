name: hub:upgrade_mundane 資源不足時の denial（D 分割後）
description: |
  Pulse-system update — upgrade_weapon is split into upgrade_mundane/
  upgrade_pure/upgrade_oni. This test exercises the mundane path
  (魂石碎片 ×3 + 100 両) with no shards. The denial narration mentions
  the missing resource.
state:
  baseline:
    completedScripts:
      - 000_intro
    weapons:
      ancestor_yaodao:
        power: 4
  sengoku-raid:
    raid: null
    metCharacters: []
inputs:
  - type: doActivity
    id: upgrade_mundane
  - type: next
assertions:
  - kind: state
    path: baseline.weapons.ancestor_yaodao.power
    eq: 4
  - kind: output
    type: narration
    present: true
    textIncludes: 魂石碎片
