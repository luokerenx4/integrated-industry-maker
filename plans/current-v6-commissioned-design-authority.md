# Restore current V6 commissioned Design authority

- Status: `completed`
- Updated: `2026-07-25`
- Related design: [[docs/design/design-programs]], [[docs/design/fab-loss-attribution]], [[docs/design/operator-workbench]], [[docs/design/experiment-workbench]], and [[docs/design/blueprint-optimization]].

## Outcome

The exact commissioned memory fab has one current V6 immutable Design authority that either produces a fully guarded promotable improvement or records an honest bounded frontier with no factory mutation, and humans and Agents reopen the same decision from the current Workbench handoff.

## Context

Run `083-simulate` is the current operating authority under `inm-sim/0.82.0` and fab-loss V6. It identifies productive-equipment input starvation as the leading compatible-run signal while preserving the new causal transport partition. The aligned `commissioned-dram-fab` Program has no current Design authority: strict pre-alpha loading correctly excludes all `27` V5 artifacts, so Workbench reports `0` valid runs and offers only the read-only Program brief.

That state is honest but incomplete. The project has a current locked five-case Benchmark, seven absolute industrial outcome guardrails, a uniform zero-regression current-best policy, and a project-local TypeScript proposal provider. The next product-owned action is therefore to create fresh V6 evidence—not to migrate old artifacts, weaken gates, or infer that the leading compatible-run signal guarantees a profitable intervention.

## Scope

### In scope

- Execute one bounded current V6 `commissioned-dram-fab` Design run from the unchanged commissioned Blueprint.
- Inspect proposal loss/case targets, locked-case results, branch transitions, capacity checks, and the final promotion patch.
- Review, apply, and resimulate only a non-empty leader Candidate that passes every existing guard; otherwise preserve the negative frontier without changing the factory.
- Keep Workbench, CLI, Studio, immutable evidence, current project documentation, and the plan index synchronized.

### Out of scope

- Loading, migrating, aliasing, or granting partial authority to V5 Design artifacts.
- Weakening the Benchmark, hard outcome thresholds, uniform zero-regression policy, Candidate review boundary, or project-local self-containment.
- Treating `083-simulate` input gaps or transport blocking as proof of recovered output.
- Adding a speculative new equipment or control family before the current proposal portfolio is evaluated.

## Acceptance

- [x] One strict V6 immutable Design run is current authority for the exact commissioned Blueprint, Program, Benchmark, catalogs, Scenario, Objective, and engine.
- [x] Every evaluated proposal names its exact Core-owned V6 loss or promotion-blocking case and records deterministic five-case evidence.
- [x] The commissioned Blueprint changes only through a reviewed `KEEP` Candidate with a non-empty promotion patch and zero current-best regression.
- [x] CLI and Studio reopen the same final authority, frontier state, evidence totals, and next action for humans and Agents.
- [x] Project validation, public inspect/design/test loops, focused/full tests, browser verification, documentation, Git, and remote verification pass.

## Work

- [x] Audit current Run `083-simulate`, the missing Design authority, proposal portfolio, and strict invalid-artifact boundary.
- [x] Execute the maximum-bounded current V6 Design run and inspect every proposal, decision, and frontier transition.
- [x] Commission a qualifying winner and produce a new compatible Run, or preserve the exact bounded negative result without factory mutation.
- [x] Synchronize shared public surfaces, durable documentation, fixtures, and tests.
- [x] Complete browser, regression, plan, Git, and remote verification.

## Findings and decisions

- 2026-07-25 — All `27` older Design artifacts are invalid under the exact V6 input identity; this plan will create new evidence rather than add a compatibility reader.
- 2026-07-25 — Workbench loss authority comes from compatible Run `083-simulate`, while the Design Program evaluates its authored `mixed-quality` driver and all five locked cases. Proposal evidence must keep those roles explicit instead of pretending the operating Run is the evaluator.
- 2026-07-25 — The seven-Candidate budget is a ceiling. Proposal exhaustion, a rejected frontier, or an empty promotion patch is a valid industrial result and must not be replaced with a speculative mutation.
- 2026-07-25 — V6 Run `059d9ae5715394ed80c84b70d0fab51640ad0e99aed1e84378b3a7dc6200154a` evaluates four eligible proposals and exhausts the unchanged seed. `9/6`, `8/5`, and `10/7` EDD variants regress aggregate score by `4.075268`, `3.503639`, and `4.199002`; four-job inspection maintenance regresses by `0.567413`. All four fail the uniform current-best boundary, and the release variants or maintenance shortcut also miss exact on-time service floors.
- 2026-07-25 — The final leader has zero promotion operations. No Candidate, Blueprint mutation, or `084-simulate` is valid; Workbench now points both surfaces at the exact exhausted run and asks for a new intervention family.

## Verification

- `bun run inm design examples/memory-fab --program commissioned-dram-fab --run --max-candidates 7 --progress ndjson --json --section summary` — immutable V6 Run `059d9ae5715394ed80c84b70d0fab51640ad0e99aed1e84378b3a7dc6200154a`; four Candidates evaluated, one seed exhaustion, `frontier-exhausted`, unchanged leader, and zero promotion operations.
- `bun run inm inspect examples/memory-fab --json --section summary` — Workbench classifies one current, zero historical, and `27` invalid runs; authority is the exact exhausted V6 result and the next action is `REVIEW EXHAUSTED DESIGN`.
- `bun run inm design examples/memory-fab --program commissioned-dram-fab --run-id 059d9ae5715394ed80c84b70d0fab51640ad0e99aed1e84378b3a7dc6200154a --json --section summary` — direct immutable reopen verifies `4/7`, `frontier-exhausted`, seed leader, and empty promotion patch.
- `bun test packages/inm-cli/src/commands.test.ts --test-name-pattern 'public inspect gives Agents and humans the same exhausted current V6 Design authority'` — `1` pass, `0` fail.
- `bun run docs:check` — `791` documentation double-links resolve.
- `bun run inm validate examples/memory-fab --json` — commissioned Blueprint `dc9909…` remains valid with `62` Devices and `17` connections.
- `bun run inm test examples/memory-fab --json` — both project fixtures pass.
- `bun run test` — all TypeScript projects, `237` tests with `1975` assertions, and all eight Ironworks project scenarios pass.
- Studio `/memory-fab` and `/memory-fab/designs/commissioned-dram-fab/runs/059d9ae5715394ed80c84b70d0fab51640ad0e99aed1e84378b3a7dc6200154a` — shared `REVIEW EXHAUSTED DESIGN` action, `1 VALID / 27 EXCLUDED`, exact four loss-targeted rejections, one exhausted leader, and no promotable design render successfully.

## Progress log

- 2026-07-25 — Plan activated from the shared `OPEN DESIGN LOOP` handoff and strict `0 valid / 27 invalid` V6 evidence state.
- 2026-07-25 — Current V6 authority created with four exact loss-targeted rejections, one node-exhaustion record, and an unchanged commissioned factory; shared surface synchronization is in progress.
- 2026-07-25 — CLI, Studio, project, focused, full, documentation, browser, Git, and remote gates pass; the plan completes without a Blueprint or production-Run mutation.

## Completion

The commissioned memory fab now has one strict current V6 Design authority instead of a loss signal with no current decision evidence. Run `059d9ae…` proves that the remaining eligible release-window and inspection-maintenance changes do not improve the exact guarded factory: all four candidates are rejected, the seed is exhausted, and no promotion patch exists. Humans and Agents reopen the same immutable negative frontier and receive the same instruction to expand the project-local intervention portfolio. The commissioned Blueprint and compatible Run `083-simulate` remain unchanged by design.
